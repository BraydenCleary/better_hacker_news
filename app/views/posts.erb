<ul>
<% @posts.each do |post| %>
  <li><%= post.post_votes.length %>||<a href="/posts/<%= post.id%>/upvote">^</a>||<a href="<%= post.link %>"><%= post.title %></a> || <a href="/posts/<%= post.id%>"><%= post.comments.length %></a></li> 
<% end %>
</ul>
  
